package main.treatment;

import main.person.Doctor;
import main.person.Patient;

public class HeartSurgery extends Surgery{
	 public HeartSurgery(Doctor doctor, Patient patient) {
	        super(doctor, patient);
	    }

	    @Override
	    public void performTreatment() {
	        System.out.println("Performing heart surgery on patient: " + patient.getName() + " by doctor: " + doctor.getName());
	    }
}
